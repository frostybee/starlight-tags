---
title: TagBadge Demo
description: Live demo of the TagBadge component
tags:
  - components
  - example
---

import { TagBadge } from 'starlight-tags/components';

This page demonstrates the `TagBadge` component, which renders a single tag as a styled badge.

## Basic Usage

Single tags with different configurations:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" />
  <TagBadge tagId="guide" />
  <TagBadge tagId="plugins" />
  <TagBadge tagId="advanced" />
</div>

```astro
import { TagBadge } from 'starlight-tags/components';

<TagBadge tagId="configuration" />
```

## Style Variants

### Solid (Default)

Filled background with the tag's color:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" variant="solid" />
  <TagBadge tagId="guide" variant="solid" />
  <TagBadge tagId="getting-started" variant="solid" />
</div>

### Outline

Bordered with transparent background:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" variant="outline" />
  <TagBadge tagId="guide" variant="outline" />
  <TagBadge tagId="getting-started" variant="outline" />
</div>

### Ghost

Subtle background tint:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" variant="ghost" />
  <TagBadge tagId="guide" variant="ghost" />
  <TagBadge tagId="getting-started" variant="ghost" />
</div>

## Sizes

### Small

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" size="small" />
  <TagBadge tagId="guide" size="small" />
</div>

### Medium (Default)

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" size="medium" />
  <TagBadge tagId="guide" size="medium" />
</div>

### Large

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" size="large" />
  <TagBadge tagId="guide" size="large" />
</div>

## With Page Count

Show how many pages use each tag:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" showCount={true} />
  <TagBadge tagId="guide" showCount={true} />
  <TagBadge tagId="example" showCount={true} />
</div>

## Without Icon

Hide the tag icon:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" showIcon={false} />
  <TagBadge tagId="guide" showIcon={false} />
  <TagBadge tagId="getting-started" showIcon={false} />
</div>

## Non-Clickable

Render as a span instead of a link:

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="configuration" clickable={false} />
  <TagBadge tagId="guide" clickable={false} />
</div>

## All Tag Types

### Content Types

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="guide" />
  <TagBadge tagId="reference" />
  <TagBadge tagId="tutorial" />
  <TagBadge tagId="example" />
</div>

### Audience

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="getting-started" />
  <TagBadge tagId="advanced" />
  <TagBadge tagId="migration" />
  <TagBadge tagId="troubleshooting" />
</div>

### Core Features

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="components" />
  <TagBadge tagId="configuration" />
  <TagBadge tagId="theming" />
  <TagBadge tagId="routing" />
</div>

### Integrations

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="plugins" />
  <TagBadge tagId="frameworks" />
  <TagBadge tagId="deployment" />
</div>

### Educational

<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; align-items: center;">
  <TagBadge tagId="js-basics" />
  <TagBadge tagId="js-functions" />
  <TagBadge tagId="js-async" />
</div>

---

See the [TagBadge documentation](/starlight-tags/components/tag-badge/) for full API reference.
